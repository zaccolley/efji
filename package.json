{
  "name": "foodflood",
  "version": "2.0.0",
  "description": "A colour matching game with food",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "serve ./src",
    "deploy": "surge --project ./src --domain foodflood.surge.sh"
  },
  "author": "Zac Colley",
  "devDependencies": {
    "surge": "^0.19.0",
    "serve": "^6.0.6"
  }
}
